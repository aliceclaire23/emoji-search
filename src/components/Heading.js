import React from 'react';

const Heading = () => {
  return (<h1>Emoji Search</h1>
    <p>Use the search bar to filter the emojis, click on an emoji to copy it to the clipboard.</p>);
};

export default Heading;
